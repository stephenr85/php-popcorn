> You are in **rushing/php-popcorn** — framework-free core for transport-agnostic invocable capabilities and self-validating strategy ladders.

This is the invocable kernel behind `rushing/laravel-popcorn` (and its `laravel-popcorn-bubble` / `laravel-popcorn-wasm` siblings). It has no Laravel dependency by design — see the `mustNotRequire: illuminate/*` constraint in `composer.json`.
